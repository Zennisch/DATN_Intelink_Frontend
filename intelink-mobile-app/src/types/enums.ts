export enum UserRole {
	ADMIN = "ADMIN",
	USER = "USER",
}

export enum UserProvider {
	LOCAL = "LOCAL",
	GOOGLE = "GOOGLE",
	GITHUB = "GITHUB",
}

export enum UserStatus {
	ACTIVE = "ACTIVE",
	INACTIVE = "INACTIVE",
	SUSPENDED = "SUSPENDED",
	PENDING = "PENDING",
}
